// Area of triangle
#include<stdio.h>
#include<conio.h>
int main()
{
	float a,h=10,b=5;
	clrscr();
	printf("Triangle base : %f & height : %f",b,h);
	a = (0.5)*b*h;
	printf("\nArea of Triangle: %f",a);
	return 0;
}